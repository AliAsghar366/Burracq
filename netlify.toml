[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Serverless functions used by PayPal checkout (create + capture orders).
[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
